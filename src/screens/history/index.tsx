import FixedContainer from '@components/FixContainer';
import React from 'react';

const History = () => {
  return <FixedContainer edges={['bottom', 'top']}></FixedContainer>;
};

export default History;
